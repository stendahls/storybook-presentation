import { useEffect } from "react";

export type ItemDetailsSlideProps = {
  type: "item-details";
};

const ItemDetailsSlide = ({

}: ItemDetailsSlideProps) => {
  useEffect(() => {
  }, []);
  return (
    <></>
  )
};

export default ItemDetailsSlide;
