import { Meta, StoryObj } from "@storybook/react";
import ImageSlide from "./ImageSlide";

const meta: Meta<typeof ImageSlide> = {
  component: ImageSlide,
  parameters: {
    layout: "fullscreen",
  }
};

export default meta;

export const Example: StoryObj<typeof ImageSlide> = {

}; 
