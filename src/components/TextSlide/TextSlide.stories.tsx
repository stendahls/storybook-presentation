import { Meta, StoryObj } from "@storybook/react";
import TextSlide from "./TextSlide";

const meta: Meta<typeof TextSlide> = {
  component: TextSlide,
  parameters: {
    layout: "fullscreen",
  }
};

export default meta;

export const Example: StoryObj<typeof TextSlide> = {

}; 
